from django.urls import path
from .views import *

urlpatterns = [
    # path('validate-email/', Validate_Email.as_view({'get': 'list', 'post': 'create'})),
    # path('sign-up/', Sign_Up.as_view()),
    # path('sign-in/', Sign_in.as_view()),
    # path('request-reset-password/', Request_reset_password.as_view()),
    # path('reset-password/', Reset_password.as_view()),
    # path('account/', GoogleView.as_view()),
]
